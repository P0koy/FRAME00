import { useEffect, useRef } from "react";
import { supabase } from "./lib/supabase.js";
import "./enhancements.css";

const ADMIN_EMAIL = "kanshoev.amika@gmail.com";
const typeLabels = { film: "Фильм", series: "Сериал", anime: "Аниме" };
const esc = v => String(v ?? "").replace(/[&<>\"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));

function imageToDataUrl(file){return new Promise((resolve,reject)=>{if(!file||!file.type.startsWith("image/"))return reject(new Error("Выберите файл изображения."));if(file.size>10*1024*1024)return reject(new Error("Изображение слишком большое. Максимум 10 МБ."));const r=new FileReader();r.onload=()=>{const img=new Image();img.onload=()=>{const scale=Math.min(1,1800/Math.max(img.width,img.height));const c=document.createElement("canvas");c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));c.getContext("2d").drawImage(img,0,0,c.width,c.height);resolve(c.toDataURL("image/webp",.88));};img.onerror=()=>reject(new Error("Не удалось обработать изображение."));img.src=String(r.result)};r.onerror=()=>reject(new Error("Не удалось прочитать файл."));r.readAsDataURL(file)})}
function loadImage(src){return new Promise((resolve,reject)=>{const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>reject(new Error("Не удалось подготовить изображение."));img.src=src})}
async function cropToPoster(src,x=50,y=50,zoom=1){const img=await loadImage(src),iw=img.naturalWidth||img.width,ih=img.naturalHeight||img.height,tw=700,th=1050,ratio=tw/th,ir=iw/ih;let cw=ir>ratio?ih*ratio:iw,ch=ir>ratio?ih:iw/ratio;const z=Math.max(1,Math.min(2.4,Number(zoom)||1));cw=Math.min(iw,cw/z);ch=Math.min(ih,ch/z);const px=Math.max(0,Math.min(100,Number(x)||50))/100,py=Math.max(0,Math.min(100,Number(y)||50))/100;const sx=Math.max(0,Math.min(iw-cw,px*(iw-cw))),sy=Math.max(0,Math.min(ih-ch,py*(ih-ch)));const c=document.createElement("canvas");c.width=tw;c.height=th;c.getContext("2d").drawImage(img,sx,sy,cw,ch,0,0,tw,th);return c.toDataURL("image/webp",.9)}
async function findWork(title,year){const exact=await supabase.from("media").select("id,title,release_year,poster_url").eq("title",title).maybeSingle();if(exact.data)return exact.data;const q=await supabase.from("media").select("id,title,release_year,poster_url").ilike("title",title).limit(10);return(q.data||[]).find(x=>!year||Number(x.release_year)===Number(year))||null}
function currentWork(){const hero=document.querySelector(".work-hero"),h1=hero?.querySelector(".work-info h1");if(!hero||!h1)return null;const y=(hero.querySelector(".work-info .eyebrow")?.textContent||"").match(/·\s*(\d{4})/);return{hero,poster:hero.querySelector(":scope > .poster"),title:h1.textContent.trim(),year:y?.[1]||null}}
function openPosterEditor({source,mediaId,mode,userId,onDone}){const o=document.createElement("div");o.className="frame-poster-editor-overlay";o.innerHTML=`<div class="frame-poster-editor"><button class="frame-editor-close" type="button">×</button><div class="frame-editor-kicker">FRAME99 · ${mode==="admin"?"АДМИН":"ПРЕДЛОЖЕНИЕ"}</div><h2>ОБРЕЗАТЬ ПОСТЕР</h2><p class="frame-editor-help">Финальный вид постера виден сразу. Перетаскивай изображение и меняй масштаб.</p><div class="frame-editor-preview-row"><div class="frame-editor-crop-wrap"><span>ФИНАЛЬНЫЙ КАДР 2:3</span><div class="frame-editor-stage"><img alt="Предпросмотр постера"/></div></div></div><div class="frame-editor-controls"><label>МАСШТАБ <strong data-zv>100%</strong><input data-z type="range" min="100" max="240" value="100"/></label></div><div class="frame-editor-actions"><button class="frame-editor-button secondary" type="button" data-reset>ЦЕНТРИРОВАТЬ</button><button class="frame-editor-button primary" type="button" data-save>${mode==="admin"?"СОХРАНИТЬ ПОСТЕР":"ОТПРАВИТЬ ПРЕДЛОЖЕНИЕ"}</button></div><p class="frame-editor-message"></p></div>`;document.body.appendChild(o);const img=o.querySelector("img"),stage=o.querySelector(".frame-editor-stage"),zi=o.querySelector("[data-z]"),zv=o.querySelector("[data-zv]"),msg=o.querySelector(".frame-editor-message");img.src=source;let x=50,y=50,z=1;const render=()=>{img.style.transform=`scale(${z})`;img.style.transformOrigin=`${x}% ${y}%`;zv.textContent=`${Math.round(z*100)}%`};render();const close=()=>o.remove();o.querySelector(".frame-editor-close").onclick=close;o.querySelector("[data-reset]").onclick=()=>{x=50;y=50;z=1;zi.value="100";render()};zi.oninput=()=>{z=Number(zi.value)/100;render()};let dragging=false,sx=0,sy=0,ox=50,oy=50;stage.onpointerdown=e=>{dragging=true;stage.setPointerCapture(e.pointerId);sx=e.clientX;sy=e.clientY;ox=x;oy=y};stage.onpointermove=e=>{if(!dragging)return;x=Math.max(0,Math.min(100,ox-(e.clientX-sx)/Math.max(1,stage.clientWidth)*100));y=Math.max(0,Math.min(100,oy-(e.clientY-sy)/Math.max(1,stage.clientHeight)*100));render()};["pointerup","pointercancel"].forEach(e=>stage.addEventListener(e,()=>dragging=false));o.querySelector("[data-save]").onclick=async()=>{const b=o.querySelector("[data-save]");b.disabled=true;msg.textContent="ГОТОВИМ ФИНАЛЬНЫЙ ПОСТЕР…";try{const cropped=await cropToPoster(source,x,y,z);if(mode==="admin"){const {error}=await supabase.rpc("admin_update_media_poster",{p_media_id:mediaId,p_poster_url:cropped,p_position_x:50,p_position_y:50});if(error)throw error}else{const {error}=await supabase.from("poster_proposals").insert({media_id:mediaId,user_id:userId,poster_url:cropped,position_x:50,position_y:50});if(error)throw error}msg.textContent=mode==="admin"?"Постер сохранён.":"Предложение отправлено администратору.";setTimeout(()=>{close();onDone?.()},550)}catch(e){msg.textContent=e.message||"Не удалось сохранить постер.";b.disabled=false}}}
function injectPoster(userRef){const w=currentWork();if(!w?.poster||w.poster.dataset.frameStableReady==="1")return;w.poster.dataset.frameStableReady="1";const admin=(userRef.current?.email||"").toLowerCase()===ADMIN_EMAIL;const noPoster=!w.poster.querySelector(".poster-image");const wrap=document.createElement("div");wrap.className="frame-poster-actions";const input=document.createElement("input");input.type="file";input.accept="image/*";input.className="frame-hidden-file";const add=document.createElement("button");add.type="button";add.className="frame-poster-action";add.textContent=admin?(noPoster?"ПОСТАВИТЬ ПОСТЕР":"ИЗМЕНИТЬ ПОСТЕР"):(noPoster?"ПРЕДЛОЖИТЬ ПОСТЕР":"ПРЕДЛОЖИТЬ ДРУГОЙ ПОСТЕР");input.onchange=async()=>{try{const source=await imageToDataUrl(input.files?.[0]);const media=await findWork(w.title,w.year);if(!media)throw new Error("Произведение не найдено в каталоге.");openPosterEditor({source,mediaId:media.id,mode:admin?"admin":"proposal",userId:userRef.current?.id,onDone:()=>window.location.reload()})}catch(e){window.alert(e.message||"Не удалось обработать изображение.")}};add.onclick=()=>input.click();wrap.append(add,input);if(admin&&!noPoster){const del=document.createElement("button");del.type="button";del.className="frame-poster-action danger";del.textContent="УДАЛИТЬ ПОСТЕР";del.onclick=async e=>{e.stopPropagation();const m=await findWork(w.title,w.year);if(!m)return;if(!window.confirm(`Удалить постер у «${m.title}»?`))return;const {error}=await supabase.rpc("admin_delete_poster",{p_media_id:m.id});if(error)return window.alert(error.message);window.location.reload()};wrap.append(del)}w.poster.appendChild(wrap)}

let topPage=null,topStatus="idle",topToken=0;
async function renderTopFive(){const home=document.querySelector(".hero"),catalog=document.querySelector(".catalog"),page=home?"home":catalog?"catalog":null;if(!page){document.querySelectorAll(".frame-top-five").forEach(x=>x.remove());topPage=null;topStatus="idle";topToken++;return}const anchor=home||document.querySelector(".catalog-head");if(!anchor)return;if(topPage===page&&(topStatus==="loading"||topStatus==="ready")&&anchor.nextElementSibling?.classList.contains("frame-top-five"))return;if(topPage===page&&topStatus==="loading")return;const token=++topToken;topPage=page;topStatus="loading";document.querySelectorAll(".frame-top-five").forEach(x=>x.remove());const {data:media,error}=await supabase.from("media").select("id,title,media_type,poster_url").limit(1000);if(token!==topToken)return;if(error||!media?.length){topStatus="idle";return}const {data:reviews}=await supabase.from("reviews").select("media_id").in("media_id",media.map(m=>m.id));if(token!==topToken)return;const counts={};(reviews||[]).forEach(r=>counts[r.media_id]=(counts[r.media_id]||0)+1);const top=media.map(m=>({...m,reviewCount:counts[m.id]||0})).filter(m=>m.reviewCount>0).sort((a,b)=>b.reviewCount-a.reviewCount||a.title.localeCompare(b.title,"ru")).slice(0,5);if(!top.length){topStatus="ready";return}if(!document.querySelector(page==="home"?".hero":".catalog")){topStatus="idle";return}if(document.querySelector(`.frame-top-five[data-frame-top-page="${page}"]`)){topStatus="ready";return}const host=document.createElement("section");host.className="frame-top-five";host.dataset.frameTopPage=page;host.innerHTML=`<div class="frame-top-head"><div><span>СООБЩЕСТВО</span><h2>ТОП 5 ПО КОЛИЧЕСТВУ РЕЦЕНЗИЙ</h2></div><p>Пять произведений, на которые сообщество оставило больше всего рецензий.</p></div><div class="frame-top-grid">${top.map((m,i)=>`<button class="frame-top-item" data-work-id="${esc(m.id)}" type="button"><span class="frame-top-rank">${i+1}</span><span class="frame-top-poster">${m.poster_url?`<img src="${esc(m.poster_url)}" alt=""/>`:`<b>99</b>`}</span><span class="frame-top-title">${esc(m.title)}</span><small>${typeLabels[m.media_type]||"Произведение"} · ${m.reviewCount} ${m.reviewCount===1?"рецензия":"рецензий"}</small><strong class="frame-top-action">ОЦЕНИТЬ →</strong></button>`).join("")}</div>`;anchor.insertAdjacentElement("afterend",host);topStatus="ready";host.querySelectorAll("[data-work-id]").forEach(btn=>btn.onclick=()=>{const m=top.find(x=>x.id===btn.dataset.workId);if(!m)return;const card=[...document.querySelectorAll(".catalog .work-card,.editorial-grid .work-card")].find(c=>c.querySelector("h3")?.textContent?.trim()===m.title);if(card){card.click();setTimeout(()=>document.querySelector(".work-hero .primary-action")?.click(),350)}else{localStorage.setItem("frame99_rate_target",m.title);[...document.querySelectorAll(".nav button")].find(b=>(b.textContent||"").trim()==="КАТАЛОГ")?.click()}})}
function consumeRateTarget(){const target=localStorage.getItem("frame99_rate_target");if(!target||!document.querySelector(".catalog"))return;const card=[...document.querySelectorAll(".catalog .work-card")].find(c=>c.querySelector("h3")?.textContent?.trim()?.toLowerCase()===target.toLowerCase());if(card){localStorage.removeItem("frame99_rate_target");card.click();setTimeout(()=>document.querySelector(".work-hero .primary-action")?.click(),350)}}
function stylePlaceholders(){document.querySelectorAll(".placeholder-poster").forEach(el=>{const mark=el.querySelector(".poster-mark");const small=el.querySelector("small");if(mark)mark.textContent="99";if(small)small.textContent="FRAME99";el.classList.add("frame99-placeholder")})}
function adminCatalog(userRef){if((userRef.current?.email||"").toLowerCase()!==ADMIN_EMAIL)return;document.querySelectorAll(".work-card").forEach(card=>{if(card.dataset.frameAdminReady==="1")return;const title=card.querySelector("h3")?.textContent?.trim();const eyebrow=card.querySelector(".eyebrow")?.textContent||"";const year=eyebrow.match(/·\s*(\d{4})/)?.[1]||null;if(!title)return;card.dataset.frameAdminReady="1";const bar=document.createElement("div");bar.className="frame-admin-card-actions";const edit=document.createElement("button");edit.type="button";edit.textContent="ПОСТЕР";const del=document.createElement("button");del.type="button";del.textContent="УДАЛИТЬ";edit.onclick=async e=>{e.stopPropagation();const m=await findWork(title,year);if(!m)return;const input=document.createElement("input");input.type="file";input.accept="image/*";input.onchange=async()=>{try{const src=await imageToDataUrl(input.files?.[0]);openPosterEditor({source:src,mediaId:m.id,mode:"admin",userId:userRef.current?.id,onDone:()=>window.location.reload()})}catch(err){window.alert(err.message)}};input.click()};del.onclick=async e=>{e.stopPropagation();const m=await findWork(title,year);if(!m)return;if(!window.confirm(`Удалить «${m.title}» из каталога вместе с оценками и рецензиями?`))return;const {error}=await supabase.rpc("admin_delete_media",{p_media_id:m.id});if(error)return window.alert(error.message);card.remove();topStatus="idle";renderTopFive()};bar.append(edit,del);card.appendChild(bar)})}
async function adminProposals(userRef){if((userRef.current?.email||"").toLowerCase()!==ADMIN_EMAIL||!document.querySelector(".admin-page"))return;let host=document.querySelector(".frame-poster-proposals");if(!host){host=document.createElement("section");host.className="admin-section frame-poster-proposals";document.querySelector(".admin-page")?.appendChild(host)}const {data}=await supabase.from("poster_proposals").select("id,poster_url,created_at").eq("status","pending").order("created_at",{ascending:false});host.innerHTML=`<div class="section-head"><div><span>ПОСТЕРЫ</span><h2>ПРЕДЛОЖЕНИЯ ПОЛЬЗОВАТЕЛЕЙ</h2></div></div>${data?.length?`<div class="frame-proposal-list">${data.map(p=>`<div class="frame-proposal"><img src="${esc(p.poster_url)}" alt=""/><div><b>Предложение постера</b><small>${new Date(p.created_at).toLocaleString("ru-RU")}</small></div></div>`).join("")}</div>`:`<div class="empty-state">Новых предложений нет.</div>`}`}
function refresh(userRef){stylePlaceholders();renderTopFive();consumeRateTarget();injectPoster(userRef);adminCatalog(userRef);adminProposals(userRef)}
export default function StableEnhancements(){const userRef=useRef(null);useEffect(()=>{let timer;supabase.auth.getSession().then(({data})=>{userRef.current=data.session?.user||null;refresh(userRef)});const{data:sub}=supabase.auth.onAuthStateChange((_e,s)=>{userRef.current=s?.user||null;topPage=null;topStatus="idle";topToken++;refresh(userRef)});const observer=new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(()=>refresh(userRef),80)});observer.observe(document.body,{childList:true,subtree:true});return()=>{sub.subscription.unsubscribe();observer.disconnect();clearTimeout(timer)}},[]);return null}
